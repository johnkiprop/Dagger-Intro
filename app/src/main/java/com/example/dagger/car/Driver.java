package com.example.dagger.car;

import javax.inject.Inject;
import javax.inject.Singleton;
//the singleton annotation is for Car like to ensure car1 and car2 only have one instance

public class Driver {
  //We don't own this class so we do not annotate with @Inject

}
